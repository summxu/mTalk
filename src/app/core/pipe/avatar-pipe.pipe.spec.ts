import { AvatarPipePipe } from './avatar-pipe.pipe';

describe('AvatarPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AvatarPipePipe();
    expect(pipe).toBeTruthy();
  });
});
